
### LOADING .sql file for DP datamodel creation
mysql -u root -p < SQLScript.sql
CREATE required user:
    CREATE USER 'dp'@'localhost' IDENTIFIED BY 'dp';


NOTE:
Will get e.g.: java.lang.NoSuchMethodException: DP.insert()
If the DP you are referencing is incorrect.


##### RESETTING GIT BRANCH
git log --oneline
  > find the commit you want
git checkout burnerbranch  (or 'develop')
git reset --hard 8a536f4
git push --force onedrive develop



##### BOOTSTRAPPING NODES ON DEV
on first runs:
run with -instance_id 0
   Go to ~/.ipfs0/config
 // Then remove all bootstrap addresses - this is intended to be the genesis node
run another with instance_id 1
   Go to ~/.ipfs1/config
   Then edit its single bootstrap address to be the PeerId of the .ipfs0 (check in ~/.ipfs0/config)
REBOOT both nodes, the dht should be able to detect each other
Add more nodes as required


##### SYMLINK to dev env
** NOTE: Ipfs default configs are established in ~/.ipfs1, 2, 3 etc...
ln -s ~/.ipfs2 .ipfs2
    > Links it to be able inspect in IDE
To test regenerating the configs
rm -rf ~/.ipfsx


############## MERGING UPDATED NABU ######
git remote add origin https://github.com/Peergos/nabu.git
git checkout -b originmaster
git reset --hard origin/master

####### MERGE or REBASE ######
## merge my latest on top of theirs
git merge develop
OR///
## merge theirs on top of mine
git checkout -b merge
git merge originmaster

## ALTERNATIVELY, REBASE
git checkout -b merge
git rebase originmaster
This moves the entire feature branch to begin on the tip of the main branch, effectively incorporating all of the new commits in main.

git status -> resolve conflicts
    > git mergetool   > optional

Then need to run tests, make any adjustments
   > adjustments include
    - Adding back my instance_id argument procssing which modifies ports in Config.java
    - swapping bitswap for Resswap
git add updated files
git commit -m "resolved conflicts"
** then merge it all back to 'develop' branch
git checkout develop
git merge merge


This error:
INE: sun.net.www.MessageHeader@594942253 pairs: {null: HTTP/1.1 404 Not Found}{Date: Wed, 06 Sep 2023 05:23:26 GMT}{Transfer-encoding: chunked}

java.lang.RuntimeException: IOException contacting IPFS daemon.

Trailer: null
   > MEANS the endpoint of the APIHandler doesnt-exist



## MUST BE RUN WITH JAVA17
* On Dev set like this, must be done before any terminal action like mvn protobuf:compile
export JAVA_HOME=`/usr/libexec/java_home -v 17`

### GENERATING PROTOBUF STUBS
pom.xml:
        *** NOTE: I chose 3.6.1 b/c: protoc --version  >  libprotoc 3.6.1
            Might need to try other versions
        <protobuf.version>3.6.1</protobuf.version>
        ...
        <extensions>
            <extension>
                <groupId>kr.motd.maven</groupId>
                <artifactId>os-maven-plugin</artifactId>
                <version>1.6.1</version>
            </extension>
        </extensions>
        ...
                    <plugin>
                        <groupId>org.xolstice.maven.plugins</groupId>
                        <artifactId>protobuf-maven-plugin</artifactId>
                        <version>0.5.1</version>
                        <extensions>true</extensions>
                        <configuration>
                            <protocArtifact>com.google.protobuf:protoc:${protobuf.version}:exe:${os.detected.classifier}</protocArtifact>
                            <protoSourceRoot>
                                ${basedir}/src/main/proto/
                            </protoSourceRoot>
                        </configuration>
                        <executions>
                            <execution>
                                <goals>
                                    <goal>compile</goal>
                                    <goal>test-compile</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
Put: src/main/proto/message.proto
mvn protobuf:compile
   > target/generated-sources/protobuf/java ... MessageOuterClass
Copy MessageOuterClass to src/main/java/protocol/resSwap/pb/
