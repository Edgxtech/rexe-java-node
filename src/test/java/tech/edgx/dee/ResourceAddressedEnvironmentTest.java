package tech.edgx.dee;

public class ResourceAddressedEnvironmentTest {

    // Test deploy a DP, returns a UUID
    //     I.e. mysql DP; mutate, retrieve
    //     I.e. Chat Server; create, send, add/remove  ; new-gen internally resource referenced
    //     I.e. Chat Server;                          ; old gen, uses custom legacy DB

    // Test call a DP
    //    provide UUID, functionid, params
    //    APIService.call(
}
